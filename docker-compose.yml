services:
  landing:
    image: nginx:alpine
    container_name: landing_prod
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
